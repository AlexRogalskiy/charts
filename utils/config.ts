import { ConfigOptions } from "../typings/types"

export const CONFIG: ConfigOptions = {
  imageOptions: {
    format: 'svg',
    imageDataOnly: true,
    showLinks: true,
    height: 800,
    width: 800
  }
}
