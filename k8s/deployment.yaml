apiVersion: apps/v1
kind: Deployment
metadata:
  name: styled-charts
  labels:
    app.kubernetes.io/name: styled-charts
    app.kubernetes.io/instance: styled-charts
    app.kubernetes.io/version: "0.0.0"
    app.kubernetes.io/component: function
    app.kubernetes.io/part-of: serverless
    app.kubernetes.io/managed-by: tilt

spec:
  replicas: 1
  selector:
    matchLabels:
      app: styled-charts
  template:
    metadata:
      labels:
        app: styled-charts
    spec:
      containers:
        - name: styled-charts
          image: styled-charts
          ports:
            - containerPort: 3000
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
